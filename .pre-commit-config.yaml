---
repos:
  # Local hooks run through `uv run` (like the justfile) so `git commit` works
  # without an activated .venv — the hook's PATH never contains .venv/bin.
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: uv run --frozen --extra dev ruff format
        language: system
        types: [python]
        pass_filenames: true

      - id: ruff-check
        name: ruff check
        entry: uv run --frozen --extra dev ruff check --fix
        language: system
        types: [python]
        pass_filenames: true

      - id: jsonld-lint
        name: JSON-LD Parser
        entry: uv run --frozen --extra dev python -c "import json, sys; [json.load(open(f)) for f in sys.argv[1:]]"
        language: system
        files: \.(json|jsonld)$
        pass_filenames: true

      - id: turtle-lint
        name: Turtle Parser
        entry: uv run --frozen --extra dev python -c "import sys; from rdflib import Graph; [Graph().parse(f, format='turtle') for f in sys.argv[1:]]"
        language: system
        files: \.ttl$
        pass_filenames: true

  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.17.2
    hooks:
      - id: markdownlint-cli2
        args: ["--config", ".markdownlint-cli2.yaml"]
